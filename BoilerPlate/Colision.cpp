#include "Colision.h"

Colision::Colision()
{
	
}

//bool Colision::Colliding()
//{
	/*
	Vector2 posAst = Ast.getPositionAst();
	Vector2 posShip = Ship.getPositionShip();

	cout << posAst.GetX() << " y " << posAst.GetY() << endl;

	float x = posAst.GetX() - posShip.GetX();
	float y = posAst.GetY() - posShip.GetY();

	float radio = Ship.getRadioShip() + Ast.getRadioAst();
	float Distancia = x * x + y * y;
	
	if (Distancia <= radio * radio)
		return true;
		*/
//}